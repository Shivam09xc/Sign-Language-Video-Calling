# ─── Fill these in AFTER your Render services are deployed ─────────────────
# Replace with your actual Render URLs (no trailing slash)

# Node.js Signaling Server URL (from Render → asl-signaling service)
VITE_SOCKET_URL=https://asl-signaling.onrender.com

# FastAPI ML Backend WebSocket URL (from Render → asl-backend service)
VITE_ML_URL=wss://asl-backend.onrender.com/ws/predict
